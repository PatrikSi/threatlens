services:
  api:
    build:
      context: ./backend
      args:
        APP_VERSION: ${APP_VERSION:-0.1.0}
        BUILD_DATE: ${BUILD_DATE:-unknown}
        VCS_REF: ${VCS_REF:-unknown}

  web:
    build:
      context: ./web
      args:
        APP_VERSION: ${APP_VERSION:-0.1.0}
        BUILD_DATE: ${BUILD_DATE:-unknown}
        VCS_REF: ${VCS_REF:-unknown}
        VITE_API_BASE_URL: ${WEB_VITE_API_BASE_URL:-/api/v1}
